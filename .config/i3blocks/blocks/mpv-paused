#!/usr/bin/env bash
# list any paused mpv instances, so I remember I have stuff paused
# waiting in the background

diff -y --suppress-common-lines <(mpv-currently-playing --socket) <(mpv-active-sockets) | wc -l
