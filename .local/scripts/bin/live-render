#!/usr/bin/env sh
# whenever a format (md/latex) file is saved, re-compile it
# this is displayed using opout (with okular), which
# supports live-reloading from disk

[ -z "$1" ] && echo "Pass an file to render as the first argument." && exit 1

find "$1" | entr compile "$1"
