#!/usr/bin/env bash

evry 1 day -create_playlists && {
	find "$PLAINTEXT_PLAYLIST_MUSIC_DIR" -maxdepth 1 -type f -name '*.m3u8' -delete
	cd "$PLAINTEXT_PLAYLIST_MUSIC_DIR" && fd . "$PLAINTEXT_PLAYLIST_PLAYLISTS" -x sh -c 'plainplay m3u {/.} > {/.}.m3u8'
}
