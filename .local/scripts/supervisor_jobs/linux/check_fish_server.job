#!/usr/bin/env bash

evry 10 minutes -check-fish-server && {
	printlog 'checking sean.fish...'
	HTTP_CODE="$(curl -L -so /dev/null -w "%{http_code}" 'https://sean.fish')"
	[[ "$HTTP_CODE" != "200" ]] && send-error "sean.fish is down"
}
