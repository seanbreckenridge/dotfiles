#!/usr/bin/env bash

evry 1 week -cache-repos && {
	printlog 'cache-repo-roots:caching locations of my git repos'
	python3 -c 'import my.config; print("\n".join(map(str, my.config.commits.roots)))' >"${XDG_CACHE_HOME?:No cache dir set}/roots.txt"
}
