#!/bin/sh
# --nvim as first arg to use nvim in current terminal
# --client connects to the emacs daemon
# otherwise, starts emacs
#
# Examples:
#    editor --nvim file
#    editor --client file
#    editor file

if [ "$1" = "--nvim" ]; then
   shift; # remove first arg
   nvim "$@"
elif [ "$1" = "--client" ]; then
   shift;  # remove first arg
   emacsclient -c "$@"
else
   emacs "$@" &
fi
